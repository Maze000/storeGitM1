import React from 'react';

function Modal({ children }) {
    return (
        <div className="modal2a">
            <div className="modal-content2a">
                {children}
            </div>
        </div>
    );
}

export default Modal;